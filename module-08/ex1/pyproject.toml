[tool.poetry]
name = "ex1"
version = "0.1.0"
description = "Loading matrix data"
authors = ["seoliver"]
package-mode = false

[tool.poetry.dependencies]
python = "^3.10"
pandas = "2.1.0"
numpy = "1.25.0"
matplotlib = "3.7.2"
python-dotenv = "1.2"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"